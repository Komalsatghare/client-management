{
  "name": "client-management-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for client management system containing backend and two frontends",
  "scripts": {
    "install:all": "npm install --legacy-peer-deps && npm install --workspace=management --legacy-peer-deps && npm install --workspace=Admin-Dashboard --legacy-peer-deps && npm install --workspace=backend --legacy-peer-deps",
    "build:management": "npm run build --workspace=management",
    "build:admin": "npm run build --workspace=Admin-Dashboard",
    "build": "npm run build --workspace=management",
    "dev:management": "npm run dev --workspace=management",
    "dev:admin": "npm run dev --workspace=Admin-Dashboard",
    "start:backend": "npm start --workspace=backend"
  },
  "workspaces": [
    "management",
    "Admin-Dashboard",
    "backend"
  ],
  "dependencies": {
    "react-router-dom": "^7.13.0"
  }
}
